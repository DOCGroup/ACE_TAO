// $Id$

project {
  libs  += CUTS

  after += CUTS

  specific {
    Release::dynamicflags += __CUTS_INLINE__
  }

  specific (vc6, vc7, vc71, vc8, nmake) {
    compile_flags += /wd4290 /wd4251
  }

  includes += $(CUTS_ROOT)
  libpaths += $(COSMIC_ROOT)/lib
}
