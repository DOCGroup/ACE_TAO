// -*- MPC -*-
// $Id$

project : orbsvcsexe, rtevent_serv, event_skel, naming {
  after += CosEC_RtEC_Based_lib
  libs += CosEC_RtEC_Based

  specific (automake) {
    includes += $(srcdir)/../lib
  } else {
    includes += ../lib
  }

  libpaths += ../lib
}
