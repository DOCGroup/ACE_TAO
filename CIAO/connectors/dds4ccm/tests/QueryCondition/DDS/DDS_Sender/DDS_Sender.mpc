// $Id$

project(*qc_sender) : aceexe, nddslib, ndds_ts_defaults {
  requires += dummy_label
  exename = DDS_Sender
  after += Base_stub

  includes += ../Base

  exeout += ../descriptors
  libs += Base_stub
  libpaths += ../descriptors

  Source_Files {
    QC_Sender.cpp
  }

  Header_Files {
  }
}

