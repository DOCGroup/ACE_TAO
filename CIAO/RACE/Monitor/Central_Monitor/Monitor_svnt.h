// $Id$
//
// ****              Code generated by the                 ****
// ****  Component Integrated ACE ORB (CIAO) CIDL Compiler ****
// CIAO has been developed by:
//       Center for Distributed Object Computing
//       Washington University
//       St. Louis, MO
//       USA
//       http://www.cs.wustl.edu/~schmidt/doc-center.html
// CIDL Compiler has been developed by:
//       Institute for Software Integrated Systems
//       Vanderbilt University
//       Nashville, TN
//       USA
//       http://www.isis.vanderbilt.edu/
//
// Information about CIAO is available at:
//    http://www.dre.vanderbilt.edu/CIAO

#ifndef CIAO_GLUE_SESSION_MONITOR_SVNT_H
#define CIAO_GLUE_SESSION_MONITOR_SVNT_H

#include /**/ "ace/pre.h"

#include "MonitorEC.h"

#if !defined (ACE_LACKS_PRAGMA_ONCE)
# pragma once
#endif /* ACE_LACKS_PRAGMA_ONCE */

#include "ciao/Container_Base.h"
#include "ciao/Context_Impl_T.h"
#include "ciao/Servant_Impl_T.h"
#include "ciao/Home_Servant_Impl_T.h"

#include "MonitorS.h"

namespace CIAO_FACET_CIAO_RACE
{
  template <typename T>
  class Execution_Time_Monitor_Servant_T
  : public virtual POA_CIAO::RACE::Execution_Time_Monitor
  {
    public:
    Execution_Time_Monitor_Servant_T (
      ::CIAO::RACE::CCM_Execution_Time_Monitor_ptr executor,
      ::Components::CCMContext_ptr ctx);

    virtual ~Execution_Time_Monitor_Servant_T (void);

    virtual void
    push_delays (
      const char * id,
      const ::CIAO::RACE::Delays & delay
      ACE_ENV_ARG_DECL_WITH_DEFAULTS)
    ACE_THROW_SPEC (( ::CORBA::SystemException));

    virtual ::CIAO::RACE::Delays *
    get_delays (
      const char * id
      ACE_ENV_ARG_DECL_WITH_DEFAULTS)
    ACE_THROW_SPEC (( ::CORBA::SystemException,
                     ::CIAO::RACE::Execution_Time_Monitor::IdNotFound));

    // Get component implementation.
    virtual CORBA::Object_ptr
    _get_component (
      ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
    ACE_THROW_SPEC (( ::CORBA::SystemException));

    protected:
    // Facet executor.
    ::CIAO::RACE::CCM_Execution_Time_Monitor_var executor_;

    // Context object.
    ::Components::CCMContext_var ctx_;
  };

  typedef Execution_Time_Monitor_Servant_T<int> Execution_Time_Monitor_Servant;
}

namespace CIAO
{
  namespace RACE
  {
    namespace CIDL_Central_Monitor_Impl
    {
      class Central_Monitor_Servant;

      class MONITOR_SVNT_Export Central_Monitor_Context
        : public virtual CIAO::Context_Impl<
            ::CIAO::RACE::CCM_Central_Monitor_Context,
            Central_Monitor_Servant,
            ::CIAO::RACE::Central_Monitor,
            ::CIAO::RACE::Central_Monitor_var
          >
      {
        public:
        // We will allow the servant glue code we generate to access our state.
        friend class Central_Monitor_Servant;

        Central_Monitor_Context (
          ::Components::CCMHome_ptr h,
          ::CIAO::Session_Container *c,
          Central_Monitor_Servant *sv);

        virtual ~Central_Monitor_Context (void);

        // Operations for Central_Monitor receptacles and event sources,
        // defined in ::CIAO::RACE::CCM_Central_Monitor_Context.

        // CIAO-specific.

        static Central_Monitor_Context *
        _narrow (
          ::Components::SessionContext_ptr p
          ACE_ENV_ARG_DECL_WITH_DEFAULTS);

        protected:
        // Methods that manage this component's connections and consumers.

        protected:
      };

      class MONITOR_SVNT_Export Central_Monitor_Servant
        : public virtual CIAO::Servant_Impl<
            POA_CIAO::RACE::Central_Monitor,
            ::CIAO::RACE::CCM_Central_Monitor,
            Central_Monitor_Context
          >
      {
        public:

        typedef ::CIAO::RACE::CCM_Central_Monitor _exec_type;

        Central_Monitor_Servant (
          ::CIAO::RACE::CCM_Central_Monitor_ptr executor,
          ::Components::CCMHome_ptr h,
          const char *ins_name,
          ::CIAO::Home_Servant_Impl_Base *hs,
          ::CIAO::Session_Container *c,
          ::CIAO::REC_POL_MAP &rec_pol_map);

        virtual ~Central_Monitor_Servant (void);

        virtual void
        set_attributes (
          const ::Components::ConfigValues &descr
          ACE_ENV_ARG_DECL);

        // Supported operations.

        // Public port operations.

        virtual ::CIAO::RACE::Execution_Time_Monitor_ptr
        provide_Monitor (
          ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));

        // Component attribute operations.

        // Operations for Receptacles interface.

        virtual ::Components::Cookie *
        connect (
          const char *name,
          CORBA::Object_ptr connection
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException,
                         ::Components::InvalidName,
                         ::Components::InvalidConnection,
                         ::Components::AlreadyConnected,
                         ::Components::ExceededConnectionLimit));

        virtual CORBA::Object_ptr
        disconnect (
          const char *name,
          ::Components::Cookie *ck
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException,
                         ::Components::InvalidName,
                         ::Components::InvalidConnection,
                         ::Components::CookieRequired,
                         ::Components::NoConnection));

        virtual ::Components::ReceptacleDescriptions *
        get_all_receptacles (
          ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));

        // Operations for Events interface.

        virtual ::Components::Cookie *
        subscribe (
          const char *publisher_name,
          ::Components::EventConsumerBase_ptr subscriber
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException,
                         ::Components::InvalidName,
                         ::Components::InvalidConnection,
                         ::Components::ExceededConnectionLimit));

        virtual ::Components::EventConsumerBase_ptr
        unsubscribe (
          const char *publisher_name,
          ::Components::Cookie *ck
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException,
                         ::Components::InvalidName,
                         ::Components::InvalidConnection));

        virtual void
        connect_consumer (
          const char *emitter_name,
          ::Components::EventConsumerBase_ptr consumer
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException,
                         ::Components::InvalidName,
                         ::Components::AlreadyConnected,
                         ::Components::InvalidConnection));

        virtual ::Components::EventConsumerBase_ptr
        disconnect_consumer (
          const char *source_name
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException,
                         ::Components::InvalidName,
                         ::Components::NoConnection));

        virtual ::Components::PublisherDescriptions *
        get_all_publishers (
          ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));

        virtual ::Components::EmitterDescriptions *
        get_all_emitters (
          ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));

        // CIAO specific operations on the servant. 
        CORBA::Object_ptr
        get_facet_executor (
          const char *name
          ACE_ENV_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));

        private:

        ::CIAO::RACE::Execution_Time_Monitor_var
        provide_Monitor_;

        const char *ins_name_;

        private:

        void
        populate_port_tables (
          ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));

        ::CORBA::Object_ptr
        provide_Monitor_i (
          ACE_ENV_SINGLE_ARG_DECL_WITH_DEFAULTS)
        ACE_THROW_SPEC (( ::CORBA::SystemException));
      };

      class MONITOR_SVNT_Export Central_Monitor_Home_Servant
        : public virtual
            ::CIAO::Home_Servant_Impl<
                ::POA_CIAO::RACE::Central_Monitor_Home,
                ::CIAO::RACE::CCM_Central_Monitor_Home,
                Central_Monitor_Servant
              >
      {
        public:

        Central_Monitor_Home_Servant (
          ::CIAO::RACE::CCM_Central_Monitor_Home_ptr exe,
          const char *ins_name,
          ::CIAO::Session_Container *c,
          ::CIAO::REC_POL_MAP &rec_pol_map);

        virtual ~Central_Monitor_Home_Servant (void);

        // Home operations.
        // Home factory and finder operations.

        // Attribute operations.
      };

      extern "C" MONITOR_SVNT_Export ::PortableServer::Servant
      create_CIAO_RACE_Central_Monitor_Home_Servant (
        ::Components::HomeExecutorBase_ptr p,
        CIAO::Session_Container *c,
        const char *ins_name,
        ::CIAO::REC_POL_MAP &rec_pol_map
        ACE_ENV_ARG_DECL_WITH_DEFAULTS);
    }
  }
}

#include /**/ "ace/post.h"

#endif /* CIAO_GLUE_SESSION_MONITOR_SVNT_H */

