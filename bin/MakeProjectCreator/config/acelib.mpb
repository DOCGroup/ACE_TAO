// -*- MPC -*-
// $Id$

project: acedefaults, acenosubsets, ace_for_tao, pkgconfig {
  libout    = $(ACE_ROOT)/lib

  specific(automake) {
    libout -= $(ACE_ROOT)/lib
    libout += $(top_builddir)/lib
  }
}
