// -*- MPC -*-
// $Id$

project {
  requires += rpc
  Define_Custom(rpcgen) {
    command          = rpcgen
    commandflags     = -C
    inputext         = .x
    source_outputext = _svc.c, _clnt.c
    header_outputext = .h
  }
}
