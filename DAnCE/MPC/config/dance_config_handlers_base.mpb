// -*- MPC -*-
// $Id$

project : ace_xml_utils, dynamicany, typecodefactory, dance_logger {
  after += XSC_XML_Handlers
  libs  += XSC_XML_Handlers
  macros   += XML_USE_PTHREADS
  includes += $(DANCE_ROOT)/tools/Config_Handlers
}
