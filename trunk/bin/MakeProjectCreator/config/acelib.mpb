// -*- MPC -*-
// $Id$

project: acedefaults, aceversion {
  after    += ACE
  libs     += ACE
  libout    = $(ACE_ROOT)/lib 

  specific(automake) {
    libout -= $(ACE_ROOT)/lib
    libout += $(top_builddir)/lib
  }
}
