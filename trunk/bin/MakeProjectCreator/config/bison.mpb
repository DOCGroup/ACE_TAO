// $Id$
project {
  Define_Custom(BISON) {
    automatic        = 1
    command          = bison
    commandflags     = -d
    inputext         = .y
    pre_extension    = .tab
    source_outputext = .c
    header_outputext = .h
  }
}
