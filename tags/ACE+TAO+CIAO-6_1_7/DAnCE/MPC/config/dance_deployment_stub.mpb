// -*- MPC -*-
// $Id$

project : pi, messaging, dance_logger {
  after += Deployment_stub
  libs  += DAnCE_Deployment_stub
  includes += $(DANCE_ROOT)
  idlflags += -I$(DANCE_ROOT)
  requires += threads
}
