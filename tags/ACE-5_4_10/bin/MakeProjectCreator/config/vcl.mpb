// -*- MPC -*-
// $Id$

project {
  requires += vcl
  macros   += ACE_HAS_VCL=1
  specific(borland) {
     lit_libs += rtl vcl
     compile_flags += -tW -tWV
  }
  verbatim(borland,macros) {
    VCL=1
  }
}
