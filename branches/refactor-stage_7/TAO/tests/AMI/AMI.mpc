project(*Server): messaging, taoserver {
  idlflags -= -Sc
  idlflags += -GC
  requires += ami

  Source_Files {
    ami_test_i.cpp
    server.cpp
  }  
}
 
project(*Client): messaging, taoserver {
  exename   = client
  idlflags -= -Sc
  idlflags += -GC
  requires += ami

  Source_Files {
    client.cpp
  }  
}

project(*Simple Client): messaging, taoserver {
  exename   = simple_client
  idlflags -= -Sc
  idlflags += -GC
  requires += ami

  Source_Files {
    simple_client.cpp
  }  
}
