// -*- MPC -*-
// $Id$

project : ifr_client, valuetype, pi, codecfactory, ciao_vcwarnings, tao_output, ccm {
  includes += $(CIAO_ROOT)
  idlflags += -I$(CIAO_ROOT)
  after += CCM_stub
  libs  += CCM_stub
}
