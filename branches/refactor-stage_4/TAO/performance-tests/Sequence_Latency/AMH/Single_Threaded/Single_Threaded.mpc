project(*AMH sequence server): server, strategies {
  idlflags += -GH
  requires += ami

  Source_Files {
    Roundtrip.cpp
    server.cpp
  }
}

project(* AMH sequence client): client, strategies {
  idlflags += -GH
  requires += ami

  Source_Files {
    TestC.cpp
    client.cpp
  }
}

