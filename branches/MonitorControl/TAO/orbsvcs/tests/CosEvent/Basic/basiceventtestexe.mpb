// -*- MPC -*-
// $Id$

project : orbsvcsexe, messaging, taoserver, event_serv, naming, svc_utils, iortable {
  after    += CosEvent_Test_Lib
  libs     += CECTests

  specific (automake) {
    includes += $(srcdir)/../lib
  } else {
    includes += ../lib
  }
}
