// -*- MPC -*-
// $Id$

project : portableserver, avoids_minimum_corba, avoids_corba_e_compact {
  after    += RTCORBA_Common
  libs     += RTCORBA_Common

  specific (automake) {
    includes += $(srcdir)/../Common
  }

  includes += ../Common
}
