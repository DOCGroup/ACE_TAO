// -*- MPC -*-
// $Id$

project : dance_logger, dance_deployment_svnt, ciao_config_handlers, ccm_stub, dance_repositorymanagerbase, naming, iortable, utils, zlib, minizip {
  requires += zlib
  after += DAnCE_RepositoryManager
  libs += DAnCE_RepositoryManager
}
