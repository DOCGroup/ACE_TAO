// -*- MPC -*-
// $Id$

project : ciaoidldefaults {
  idlflags += -Gsv

  Modify_Custom (IDL) {
    optional(source_pre_extension) {
      commandflags(-Gsv) += _svnt
    }
    
    optional(header_pre_extension) {
      commandflags(-Gsv) += _svnt
    }
    
    optional(generic_outputext) {
      commandflags(-Gsv) += E.idl
    }
  }
}

